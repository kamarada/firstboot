#!/bin/sh
mkdir ~/.config

touch ~/.config/kamarada-firstboot
while [ -e ~/.config/kamarada-firstboot ]
do
    gedit ~/.bashrc
done

sudo sed -i 's/DEFAULT_WM="openbox"/DEFAULT_WM="gnome"/g' /etc/sysconfig/windowmanager
sudo systemctl restart display-manager
